\documentclass[11pt,a4paper,openright,openany]{report}
% Change margins, papersize, etc of the document
\usepackage[
  inner=28mm,% left margin on an odd page
  outer=41mm,% right margin on an odd page
  ]{geometry}
% Select encoding of your inputs.
\usepackage[utf8]{inputenc}
% Make latex understand and use the typographic rules of the language used in the document.
\usepackage[danish,english]{babel}
%Aetting Font to Palatino
\usepackage[sc]{mathpazo}
\linespread{1.05}         % Palatino needs more leading (space between lines)
\usepackage[T1]{fontenc} % Choose the font encoding
\usepackage{amssymb}    %Even more symbols

%Colors
\usepackage{color}
\usepackage[table,xcdraw]{xcolor}

%Package for creating captions and set up how captions are displayed
\usepackage{caption}
\captionsetup{%
  font=footnotesize,% set font size to footnotesize
  labelfont=bf % bold label (e.g., Figure 3.2) font
}
\usepackage{subcaption}

%Packages for creating basic graphics
\usepackage{float} %Package for placement
\usepackage{listings}
\definecolor{lbcolor}{rgb}{0.9,0.9,0.9}%backgroundcolor for listings
\lstset{%settings for listings
	backgroundcolor=\color{lbcolor},
	tabsize=4,
	rulecolor=,
}
\renewcommand{\lstlistingname}{Code-block}%Rename listing to code-block
\usepackage{graphicx}
\usepackage{wrapfig}%Package for creating wrapfigures
\usepackage{pdfpages}
\usepackage{array,booktabs}%make tables look pretty

% Header and footer
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} %delete everything
\renewcommand{\headrulewidth}{0pt} %remove the horizontal line in the header
\fancyhead[RE]{\small\nouppercase\leftmark} %even page - chapter title
\fancyhead[LO]{\small\nouppercase\rightmark} %uneven page - section title
\fancyhead[LE,RO]{\thepage} %page number on all pages

% Do not stretch the content of a page. Instead,
% insert white space at the bottom of the page
\raggedbottom

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Environments
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
\usepackage{amsmath}% Defines new environments such as equation, align and split
\usepackage{framed}
\usepackage[framed]{ntheorem}


%Creating links in document
\usepackage[hidelinks]{hyperref}
% cleverref
\usepackage[capitalise,noabbrev]{cleveref}
\crefname{listing}{Code-block}{Code-block}
\Crefname{listing}{Code-block}{Code-block}
\crefname{appsec}{Appendix}{Appendices}
\crefname{appsubsec}{Appendix}{Appendices}

\usepackage[toc,page]{appendix}

\setcounter{secnumdepth}{2}%Setting how deep the numbering of sections should go
\setcounter{tocdepth}{2}%Setting how deep the Table of contents should go
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Page Layout
% http://en.wikibooks.org/wiki/LaTeX/Page_Layout
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% Modify how \chapter, \section, etc. look
% The titlesec package is very configurable
\usepackage{titlesec}
%\titleformat{\chapter}[display]{\normalfont\huge\bfseries}{\chaptertitlename\ \thechapter}{20pt}{\Huge}
\titleformat{\chapter}{\normalfont\huge}{\thechapter}{20pt}{\Huge}
\titleformat*{\section}{\normalfont\Large\bfseries}
\titleformat*{\subsection}{\normalfont\large\bfseries}
\titleformat*{\subsubsection}{\normalfont\normalsize\bfseries}
%\titleformat*{\paragraph}{\normalfont\normalsize\bfseries}
%\titleformat*{\subparagraph}{\normalfont\normalsize\bfseries}
\titlespacing*{\chapter}{0pt}{0pt}{40pt}

% Clear empty pages between chapters
\let\origdoublepage\cleardoublepage
\newcommand{\clearemptydoublepage}{%
  \clearpage
  {\pagestyle{empty}\origdoublepage}%
}
\let\cleardoublepage\clearemptydoublepage


% Enable arithmetic with length. Useful when
% typesetting the layout.
\usepackage{calc}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Bibliography
% http://en.wikibooks.org/wiki/LaTeX/Bibliography_Management
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\usepackage[backend=bibtex,
  bibencoding=utf8
  ]{biblatex}
\addbibresource{bib/mybib}
\usepackage[nottoc]{tocbibind}



\usepackage{lastpage}% page number of the last page

% Enable hyperlinks and insert info into the pdf-file.
%Hypperref should be loaded as the lastpackage
\usepackage{hyperref}
\hypersetup{%
	pdfpagelabels=true,%
	plainpages=false,%
	pdfauthor={NAME},%
	pdftitle={TITLE},%
	pdfsubject={SUBTITLE},%
	bookmarksnumbered=true,%
	colorlinks=false,%
	citecolor=black,%
	filecolor=black,%
	linkcolor=black,% you should probably change this to black before printing
	urlcolor=black,%
	pdfstartview=FitH%
}
